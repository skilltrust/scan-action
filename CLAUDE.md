# scan-action

Be extremely concise. Sacrifice grammar for the sake of concision.

GitHub Action wrapping the hosted scanner (`skillmoss-go`). Shell + composite action.

- Engineering docs: `docs/INDEX.md`
- Specs & designs: `docs/specs/`
- Plans: `docs/plans/`
- Dogfood logs: `docs/dogfood/`
- Action entrypoint: `action.yml`

Owned SPs: SP5 (scan-action). Cross-component flow with SP4 (`skillmoss-go`) PR bot documented at `../docs/architecture/`.
